import { SetActiveClassDirective } from './set-active-class.directive';

describe('SetActiveClassDirective', () => {
  it('should create an instance', () => {
    /* const directive = new SetActiveClassDirective();
    expect(directive).toBeTruthy(); */
  });
});
