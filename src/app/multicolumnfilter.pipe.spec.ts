import { Multicolumnfilter } from './multicolumnfilter.pipe';

describe('MulticolumnfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new Multicolumnfilter();
    expect(pipe).toBeTruthy();
  });
});
