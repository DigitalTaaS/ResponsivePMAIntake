import { PmataxoncodePipe } from './pmataxoncode.pipe';

describe('PmataxoncodePipe', () => {
  it('create an instance', () => {
    const pipe = new PmataxoncodePipe();
    expect(pipe).toBeTruthy();
  });
});
