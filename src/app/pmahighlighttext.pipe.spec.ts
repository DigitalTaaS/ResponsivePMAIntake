import { PmahighlighttextPipe } from './pmahighlighttext.pipe';

describe('PmahighlighttextPipe', () => {
  it('create an instance', () => {
    const pipe = new PmahighlighttextPipe();
    expect(pipe).toBeTruthy();
  });
});
